def greeting(name, surname, age)
  if age < 18
    "Hello #{name} #{surname}. You're under 18, but it's never too early to start programming"
  else
    "Hello #{name} #{surname}. it's time to get down to business !"
  end
end
